# pixi_swig_py
A test repository for building C extensions with swig, cmake and pixi

> `simple` - compiles with build.sh and I can run the test_c_array.py

> `cmake` - compiles with pixi run build-cmake. Fails with test_c_array.py

Why one fails and one works - I can't work out. Will investigate more.
